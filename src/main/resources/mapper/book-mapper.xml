<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 해당 파일에 모든 쿼리문을 작성 -->
<!-- namespace에는 xml파일과 연결할 Mapper interface 파일명을 패키지를 포함하여 적어주세요 -->
<mapper namespace="com.green.book_shop.book.mapper.BookMapper">
    <resultMap id="book" type="com.green.book_shop.book.dto.BookDTO">
        <id     column="BOOK_NUM"       property="bookNum" />
        <result column="BOOK_TITLE"     property="bookTitle" />
        <result column="AUTHOR"         property="author" />
        <result column="BOOK_PRICE"     property="bookPrice" />
        <result column="BOOK_STOCK"     property="bookStock" />
        <result column="BOOK_INTRO"     property="bookIntro" />
        <result column="PUBLISH_DATE"   property="publishDate" />
        <result column="CATE_NUM"       property="cateNum" />
        <!--<association property=""></association> 1 : 1 관계 설정
        <collection property=""></collection>  1 : N 관계 설정    -->
        <!--resultMap에서는 같은 파일일 경우엔 연결한 xml id를 적어주면 되지만
        다른 파일일 땐 namespace.id를 resultMap에 적음 -->
        <collection property="bookImgList" resultMap="com.green.book_shop.book.mapper.BookImgMapper.bookImg"/>
    </resultMap>

    <!--신상 도서 등록 쿼리-->
    <insert id="insertBook">
        INSERT INTO SHOP_BOOK (
            BOOK_NUM
            , BOOK_TITLE
            , BOOK_PRICE
            , AUTHOR
            , BOOK_INTRO
            , PUBLISH_DATE
            , CATE_NUM
        ) VALUES(
            #{bookNum}
            , #{bookTitle}
            , #{bookPrice}
            , #{author}
            , #{bookIntro}
            , #{publishDate}
            , #{cateNum}
        )
    </insert>

    <!--도서목록 & 이미지 조회-->
    <select id="selectBookList" resultMap="book">
        SELECT S.BOOK_NUM
            , BOOK_TITLE
            , BOOK_PRICE
            , UPROAD_FILE_NAME
        FROM SHOP_BOOK S, BOOK_IMG I
        WHERE S.BOOK_NUM = I.BOOK_NUM
        AND IS_MAIN = 'Y'
        ORDER BY BOOK_NUM DESC
    </select>

    <!--도서 상세 & 대표이미지, 상세이미지 조회하는 쿼리-->
    <select id="selectBookDetail" resultMap="book">
        SELECT S.BOOK_NUM
            , BOOK_TITLE
            , BOOK_PRICE
            , AUTHOR
            , BOOK_INTRO
            , UPROAD_FILE_NAME
            , IS_MAIN
          FROM SHOP_BOOK S, BOOK_IMG I
         WHERE S.BOOK_NUM = I.BOOK_NUM
           AND S.BOOK_NUM = #{bookNum}
    </select>

    <!--최대 도서 번호 조회-->
	<select id="getNextBookNum" resultType="int">
        SELECT IFNULL(MAX(BOOK_NUM), 0) + 1
        FROM SHOP_BOOK
    </select>



</mapper>































